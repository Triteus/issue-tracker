import { PriorityIconNamePipe } from './priority.pipe';

describe('PriorityPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorityIconNamePipe();
    expect(pipe).toBeTruthy();
  });
});
