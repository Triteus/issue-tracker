import { TicketPathNamePipe } from './ticket-path-name.pipe';

describe('TicketPathNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TicketPathNamePipe();
    expect(pipe).toBeTruthy();
  });
});
